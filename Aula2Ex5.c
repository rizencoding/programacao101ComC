#include <stdio.h>

int main(){
    int b, h;
    printf("Informe a base: ");
    scanf("%d", &b);
    printf("Informe a altura: ");
    scanf("%d", &h);
    int mult = b * h;
    printf("A multiplicacao eh: %d", mult);
    return 0;
}
